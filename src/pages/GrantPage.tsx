
import { useDocumentTitle } from '../hooks/useDocumentTitle';

export const GrantPage = () => {
  useDocumentTitle('Grants');
  
  return (
    <div>GrantPage (Prerequisite Need to develop mobile app moduel for cooks before proceeding here) </div>
  )
}
